<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="hr_holidays_attendance.hr_employee_view_form_inherit" model="ir.ui.view">
        <field name="name">hr.holidays.attendance.employee.view.form.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="priority" eval="120"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <field name="total_overtime" invisible="1"/>
                <button
                    name="%(hr_holidays_attendance.hr_leave_allocation_overtime_manager_action)d"
                    string="Deduct Extra Hours"
                    type="action"
                    groups="hr_holidays.group_hr_holidays_user,hr.group_hr_user"
                    context="{'default_employee_id': id, 'deduct_extra_hours': True}"
                    attrs="{'invisible': [('total_overtime', '&lt;=', 1)]}"/>
            </xpath>
        </field>
    </record>
    <record id="view_employee_tree_inherit_expense" model="ir.ui.view">
        <field name="name">hr.employee.tree.expense</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="before">
                <field name="remaining_leaves"/>
            </xpath>
        </field>
    </record>
</odoo>
